{
	"name": "backend",
	"module": "src/index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"start": "bun run src/index.ts",
		"dev": "bun run --watch src/index.ts",
		"generate": "bun run src/generator/generate.ts",
		"generate:test": "bun run src/generator/generate.ts --skip-images",
		"lint": "tsgo --noEmit && biome check",
		"format": "biome check --write"
	},
	"dependencies": {
		"hono": "^4.7.10",
		"sharp": "^0.34.5",
		"viem": "^2.30.0"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.11",
		"@types/sharp": "^0.32.0",
		"@typescript/native-preview": "^7.0.0-dev.20260104.1"
	}
}
